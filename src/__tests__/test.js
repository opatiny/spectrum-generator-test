'use strict';

const spectrumGeneratorTest = require('..');

describe('spectrum-generator-test test', () => {
    it('Something to test', () => {
        expect(spectrumGeneratorTest()).toBe(true);
    });
});
